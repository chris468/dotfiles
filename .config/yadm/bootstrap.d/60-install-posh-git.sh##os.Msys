#!/bin/bash

function print_error {
    echo $@ >&2
}

pwsh -c import-module ~/scoop/apps/posh-git/current/posh-git >/dev/null \
    || (scoop bucket add extras \
        && scoop install posh-git) \
    || print_error "Failed to install posh-git"

