{{- if eq "linux" .chezmoi.os }}
ASDF_DIR="{{ .paths.asdf }}"
ASDF_DATA_DIR="$ASDF_DIR"
__asdf="$ASDF_DIR/asdf.sh"
if [ -f "$__asdf" ]
then
  . "$__asdf"
  . "$ASDF_DIR/completions/asdf.bash"
fi

unset __asdf
{{- end }}
