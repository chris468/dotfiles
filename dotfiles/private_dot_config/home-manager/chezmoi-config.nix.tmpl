{{-
  $arch := .chezmoi.arch | get (dict
    "arm64" "aarch64"
    "amd64" "x86_64"
  ) | default .chezmoi.arch
-}}
{
  username = "{{ .chezmoi.username }}";
  homeDirectory = "{{ .chezmoi.homeDir }}";
  system = "{{ $arch }}-{{ .chezmoi.os }}";
  toolsRoot = {{ joinPath .chezmoi.homeDir .path.dataHome "chris468" "tools" }};
}
