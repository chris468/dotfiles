{ config, lib, pkgs, ... }: {
  config = lib.mkIf config.tiers.essential.enable {
    home.packages = with pkgs; # packages
      [
{{- includeTemplate "tools-packages" (dict "category" "essential" "manager" "nix" "chezmoi" .chezmoi) | trimSuffix "\n" | nindent 8 }}
      ];
  };
}
