{ config, lib, pkgs, ... }: {
  config = lib.mkIf config.tiers.kubernetes.enable {
    home.packages = with pkgs; # packages
      [
{{- includeTemplate "tools-packages" (dict "category" "kubernetes" "manager" "nix" "chezmoi" .chezmoi) | indent 8 }}
      ];
  };
}
