{ config, lib, pkgs, ... }: {
  config = lib.mkIf config.tiers.azure.enable {
    home.packages = with pkgs; # packages
      [
{{- includeTemplate "tools-packages" (dict "category" "azure" "manager" "nix" "chezmoi" .chezmoi) | indent 8 }}
      ];
  };
}
