## Development Environment

Assume Claude has been started from the root of the repo and already has all of the direnv
environment loaded. Warn if you notice that `.envrc` has changed since Claude was launched.
