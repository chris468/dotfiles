chezmoi:template:missing-key=zero
{{- template "set-environment" (dict
        "environment" .environment
        "persist" "-persist"
        "command" (joinPath .paths.localInstallSbin "configure-env.ps1" | quote)
    )
-}}
